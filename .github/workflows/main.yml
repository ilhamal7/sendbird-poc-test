name: Execute_PoC
on: [push, workflow_dispatch]

jobs:
  test-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Pod Install
        run: |
          pod install --repo-update
          echo "CEK VERSI YANG TERINSTALL:"
          cat Podfile.lock | grep SendbirdAIAgentMessenger
